---
"mobx-view-model": major
---

renamed methods for `ViewModelStore` and `ViewModelStoreBase` `linkComponents` -> `link`, `unlinkComponents` -> `unlink`.   
It helps to link\unlink not only React components.
