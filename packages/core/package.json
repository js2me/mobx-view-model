{
  "name": "mobx-view-model",
  "version": "10.2.1",
  "scripts": {
    "clean": "rimraf dist",
    "ts:check": "tsc --noEmit --project tsconfig.check.json",
    "ts-test-types:check": "tsc --noEmit --project tsconfig.test.json",
    "check": "pnpm run ts:check && pnpm ts-test-types:check",
    "prebuild": "pnpm run clean",
    "build:watch": "pnpm build && nodemon --watch src --ext ts,tsx --exec \"pnpm build\"",
    "build": "vite build && node ./scripts/patch-dist-package-json.mjs",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest watch --config vitest.config.ts",
    "test:coverage": "vitest run --config vitest.config.ts --coverage"
  },
  "keywords": [
    "mobx",
    "view-model",
    "view",
    "model",
    "vm",
    "mvvm",
    "mvc",
    "mvp",
    "ViewModel",
    "ModelViewModel"
  ],
  "author": "js2me",
  "license": "MIT",
  "description": "MobX view models core (framework-agnostic)",
  "bugs": {
    "url": "https://github.com/js2me/mobx-view-model/issues"
  },
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./src/index.ts",
  "homepage": "https://github.com/js2me/mobx-view-model",
  "repository": {
    "type": "git",
    "url": "https://github.com/js2me/mobx-view-model"
  },
  "publishConfig": {
    "directory": "dist"
  },
  "dependencies": {
    "mobx-view-model-react": "workspace:^10.2.1",
    "yummies": "^7.10.0"
  },
  "peerDependencies": {
    "mobx": "^6.12.4",
    "react": "^18.0.0 || ^19.0.0"
  },
  "peerDependenciesMeta": {
    "react": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "jsdom": "^26.1.0",
    "mobx-view-model-react": "workspace:^10.2.1"
  }
}
